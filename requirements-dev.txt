# Additional dependencies for running the test suite (pytest).
# Install alongside requirements.txt: pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest~=8.3
pytest-mock~=3.14
pytest-cov~=6.0
